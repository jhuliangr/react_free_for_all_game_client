export const CANVAS_W = 800;
export const CANVAS_H = 600;
export const WORLD_SIZE = 2000;

export const SERVER_NEARBY_RADIUS = 500;
export const SCALE_X = CANVAS_W / (SERVER_NEARBY_RADIUS * 2);
export const SCALE_Y = CANVAS_H / (SERVER_NEARBY_RADIUS * 2);
